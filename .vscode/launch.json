{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Flask - Local",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/hr_project/run.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONUNBUFFERED": "1"
            }
        }
    ]
}